@using WarehouseManagement.Core.ViewModels
@using WarehouseManagement.Core.ViewModels.Common
@model List<WarehouseManagement.Core.ViewModels.BrandListViewModel>

@{
    ViewData["Title"] = "Brands";
}

<h2>Brands</h2>

<a class="btn btn-primary mb-3" asp-action="Create">+ Add Brand</a>

<!-- Filter Form -->
<form asp-action="Index" method="get" class="form-inline mb-3">
    <input type="text" name="search" class="form-control mr-2" placeholder="Search by name" value="@Context.Request.Query["search"]" />

    <button type="submit" class="btn btn-secondary">Filter</button>
</form>

<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>Name</th>
            <th>Description</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var Brand in Model)
        {
            <tr>
                <td>@Brand.Name</td>
                <td>@Brand.Description</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@Brand.BrandID" class="btn btn-sm btn-warning">Edit</a>
                    <form asp-action="Delete" asp-route-id="@Brand.BrandID" method="post" class="d-inline">
                        @Html.AntiForgeryToken()
                        <button type="submit" class="btn btn-sm btn-danger" onclick="return confirm('Are you sure?');">Delete</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
